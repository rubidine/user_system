- - The Login Filter
  - - should return the current user from the session
    - should have a filter to require login
    - should stop processing if user is disabled
    - should mix in class method to require a particular user
    - - When email validation is turned on
      - - should stop processing if user is not verified
- - A class incliding the login filters
  - - can request only certain users to have access to controller
    - can request only certain users to have access to certain action(s)
